## 1. 数据库基础切换

- [x] 1.1 彻底移除后端中 MySQL 驱动依赖、方言配置与 profile 入口，仅保留 PostgreSQL JDBC 依赖。
- [x] 1.2 更新 `dev` 等环境的数据源配置，确保默认连接 PostgreSQL 且连接池参数可用。
- [x] 1.3 梳理数据库相关启动配置与自动装配，删除 MySQL 相关兼容分支并消除硬编码依赖。

## 2. 持久层 SQL 与映射适配

- [x] 2.1 扫描并修正 MyBatis Mapper 中的 MySQL 专有语法（关键字、函数、分页、类型转换）。
- [x] 2.2 调整实体字段与 JDBC 类型映射策略，明确布尔、时间、自增主键在 PostgreSQL 下的对应关系。
- [x] 2.3 校验事务边界相关代码，确保 PostgreSQL 下异常回滚语义与现有行为一致。

## 3. 初始化脚本与数据引导

- [x] 3.1 将建表与约束脚本重写为 PostgreSQL 语法，并支持空库初始化。
- [x] 3.2 将基础数据导入脚本改为 PostgreSQL 兼容写法，保证重复执行具备幂等或可控更新能力。
- [x] 3.3 删除 MySQL 初始化脚本、执行入口与模板，避免误执行。
- [x] 3.4 对初始化后的库结构执行一次映射校对，确认关键列类型满足应用读写需求。

## 4. AI 原生能力基础补齐

- [x] 4.1 为核心 AI 数据表补充向量字段与索引预留，明确向量维度与相似度策略约束。
- [x] 4.2 为会话/上下文相关数据结构引入 JSONB 存储并补充可查询字段约定。
- [x] 4.3 为知识文本检索预留全文检索索引结构，支持后续混合检索接入。

## 5. 迁移落地与发布准备

- [x] 5.1 形成“仅 PostgreSQL”迁移步骤说明（执行顺序、数据迁移、版本回退与备份恢复路径）。
- [ ] 5.2 在预发环境完成核心业务路径联调并记录阻塞项（启动、登录、查询、写入、事务）。（开发环境已完成全链路验证，历史阻塞“序列未对齐导致新增失败”已修复）
- [x] 5.3 增加 AI 基础能力联调项（向量检索、JSONB 查询、全文检索）并沉淀接入规范。
- [x] 5.4 清理遗留 MySQL 专属文档/模板配置与部署参数，更新团队数据库接入说明为 PostgreSQL 基线。
- [x] 5.5 本地启动 PostgreSQL Docker 镜像用于开发，完成应用连通性确认，并配置/验证 PostgreSQL MCP 可用性。
- [x] 5.6 修复 PostgreSQL 初始化后主键序列未对齐问题，并验证新增写入链路（岗位、参数）可用。
