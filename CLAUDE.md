# RuoYi AI Quick Starter — Agent 指令

- 需求与变更流程：优先走 Comet（`/comet`）或 OpenSpec（详见 `openspec/AGENTS.md` 与 `docs/comet-workflow.md`）。
- 发版：遵循 `.cursor/skills/ruoyi-release`（`/release`）。
- 阶段守卫：`.cursor/rules/comet-workflow-guard.mdc` 常驻生效，不要绕过阶段直接宣称完成。
